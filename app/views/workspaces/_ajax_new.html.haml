-remote_form_for workspace, :url => workspace_path, :html => {:method => :get} do |f|
  .row
    =f.text_field :name, :class => "text"
    .help Ex: <strong>my_new_workspace</strong>
  / .row.checkboxes
  /   %label
  /     =check_box_tag :allow_scope_by_current_project
      
  .buttons
    =submit_tag "Create workspace"