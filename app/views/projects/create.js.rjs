if @new_project
  page.insert_html :bottom, "projects", :partial => "projects/project", :locals => {:project => @new_project}
  page.visual_effect :shake, "project_#{@new_project.id}"
  page[:project_form].reset 
end
