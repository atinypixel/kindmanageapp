%div[notes]

  = link_to 'Show', object_path(notes)
  |
  = link_to 'Edit', edit_object_path(notes)
  |
  = link_to 'Destroy', object_path(notes), :confirm => 'Really destroy notes?', :method => :delete
